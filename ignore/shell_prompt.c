#include "shell.h"

/**
 * show_prompt - Function that makes sure the prompt is displayed always.
 * @Jah-Isaac_shell#: The way the prompt displays(Custom-made).
 * Return: Always show our prompt at all times to indicate success.
 */

void show_prompt(void)
{
	shell_printf("Jah-Isaac_shell# ");
}
